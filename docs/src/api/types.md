# Types

All exported types: the array backends, multi-field containers, topologies,
boundary conditions, thread backends, and kernel-region metadata.

```@autodocs
Modules = [HaloArrays]
Order = [:type]
Private = false
```
