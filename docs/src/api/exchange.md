# Halo exchange & boundary conditions

Filling ghost cells: the exchange/synchronize API, per-field boundary conditions,
and coupled (characteristic) boundary conditions.

```@autodocs
Modules = [HaloArrays]
Pages = ["halo_exchange.jl", "boundary.jl"]
Order = [:function]
Private = false
```
