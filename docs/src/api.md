# API reference

All exported types and functions. In the REPL, `?name` shows the same
docstrings (e.g. `?synchronize_halo!`).

```@autodocs
Modules = [HaloArrays]
Private = false
Order = [:type, :function]
```
