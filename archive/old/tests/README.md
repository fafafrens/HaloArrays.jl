# Legacy Tests

These files are older exploratory MPI/topology tests that are not included by
`test/runtests.jl`.

They are kept here for reference while the active coverage lives under
`test/`. If a scenario in this directory is still important, fold it into one
of the active test files and run it through `test/runtests.jl`.
